# ==============================================================================
# Redis Service Configuration
# ==============================================================================

# Redis host port binding for local dev inspection (empty = internal to docker network only)
REDIS_HOST_PORT=127.0.0.1:6380:6379
